On lance le binaire sans rien l'usage pop ./level10 file host
On regarde le binaire avec strings on voir un %s:6969
On en deduit que il se co sur un host sur le port 6969
On essait avec le fichier token on a pas les droit
En regardant dans le binaire on voit que la fonction access est appelé et en se renseignant on comprend que ce n'est pas le suid qui est pas testé mais les droit reel du fichier.
En se renseignant un peu on voit qu'on pet bypass access avec une race condition, on fait donc un .c puis on execute :
while true; do nc -lv 127.0.0.1 6969;done
et while true; do ./level10 /tmp/prt/link 127.0.0.1; done
