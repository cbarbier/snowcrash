# reversing the binary we found that getenv("LOGNAME")
# at the end there is system("echo %s") where %s is the 
# variable pointing to "LOGNAME"
# so we update the var to "lol && /bin/bash"
# Et voila.