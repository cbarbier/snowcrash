# Find a level05's file in /var/mail
*/2 * * * * su -c "sh /usr/sbin/openarenaserver" - flag05
# Looks like a cron line
# cat /usr/sbin/openarenaserver
# It runs all the files in /opt/openaserver/ directory with flag05 rights
# Copy the exploit.sh script in it
# Don't forget to chmod 777 the script
# Just wait 2 minutes